# YOLOv12 人脸识别项目配置文件
# 复制此文件为 .env 并根据实际情况修改配置
#
# 跨平台支持说明：
# - 路径推荐使用正斜杠
# - 可以使用相对路径或绝对路径
# - 支持环境变量展开（如 $HOME 或 ~）

# ==================== 数据集路径 (必须配置) ====================
DATASET_ROOT=       # 数据集根目录路径

# ==================== 模型路径 (必须配置) ====================
BEST_MODEL_PATH=    # 最佳模型权重文件路径
LAST_MODEL_PATH=    # 最后一个epoch的模型权重文件路径（可选）

# ==================== 视频路径 (必须配置) ====================
VIDEO_PATH=         # 视频路径 (设置为 "0" 可使用摄像头)

# ==================== 训练配置 (可选) ====================
EPOCHS=100          # 训练轮数
BATCH_SIZE=6        # 批次大小
IMG_SIZE=640        # 图像尺寸
DEVICE=0            # 设备选择 (GPU编号，使用CPU请改为 "cpu")

# SwanLab项目配置
SWANLAB_PROJECT=YOLOv12-WIDER-FACE  # SwanLab项目名称
SWANLAB_EXP_NAME=yolo12n-train-100e  # SwanLab实验名称

# ==================== 推理参数 (可选) ====================
CONF_THRESHOLD=0.5  # 置信度阈值
IOU_THRESHOLD=0.5   # IOU阈值

# ==================== 导出配置 (可选) ====================
ONNX_OPSET=12       # ONNX操作集版本
ONNX_SIMPLIFY=True  # 是否简化ONNX模型

# ==================== 模型类型 (可选) ====================
MODEL_TYPE=yolo12n.pt  # 预训练模型类型（用于从头训练）
